keys =['Ten','Twenty','Thirty']
Value = [10,20,30]
combined = dict(zip(keys,Value))
print(combined)

#zip = zip is the function that takes iterables and iterators generate the series of tuples containing
# elements from each iterables.